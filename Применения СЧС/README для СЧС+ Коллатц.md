# Анализ последовательности Коллатца через Структурную Числовую Симметрию (СЧС)  
**Автор**: Ющенко Михаил Юрьевич  
**Год**: 2025

**Язык реализации**: Julia 1.10+  

---

## 🔍 **1. Назначение**

Программа предназначена для **исследования гипотезы Коллатца** с применением метода **Структурной Числовой Симметрии (СЧС)**. Она:

- Генерирует последовательность Коллатца от произвольного начального числа (включая числа с тысячами цифр).
- На каждом шаге применяет СЧС-трансформацию:
  - Разбивает число `N` на `m` частей.
  - Умножает каждую часть на `k`, сохраняя длину.
  - Сравнивает результат (`PQ`) с `N × k` (`NK`).
- Классифицирует совпадения: **полное**, **начало+конец**, **только начало**, **только конец**, **нет совпадений**.
- Визуализирует динамику процесса в виде **научного графика** и **видеоряда**.
- Сохраняет все результаты в структурированных файлах без вывода в терминал.

> 💡 **Цель**: продемонстрировать наличие скрытых числовых структур в хаотичной последовательности Коллатца через СЧС.

---

## 🧠 **2. Концепция СЧС в контексте Коллатца**

СЧС — это **не алгоритм**, а **наблюдаемое свойство десятичной системы счисления**, при котором:
- Число может быть разбито на части,
- Части преобразованы (умножены, сдвинуты и т.п.),
- Их конкатенация **частично или полностью совпадает** с результатом глобального преобразования числа.

В данном коде проверяется гипотеза:
> **Существуют ли регулярные совпадения между локальным (по частям) и глобальным (всё число) умножением на `k` в последовательности Коллатца?**

---

## ⚙️ **3. Основные компоненты**

### 3.1. Входные параметры

| Параметр           | Тип       | Значение по умолчанию | Описание |
|--------------------|-----------|------------------------|---------|
| `N_START`          | `BigInt`  | `big"9"^big"999"`    | Начальное число последовательности Коллатца. Поддерживает числа любой длины. |
| `M_SCS`            | `Int`     | `2`                    | Количество частей при разбиении числа. |
| `K_SCS`            | `Int`     | `3`                    | Множитель для СЧС-преобразования. |
| `MAX_ITERATIONS`   | `Int`     | `1000`                  | Максимальное число шагов (защита от бесконечного цикла). |

> ⚠️ **Рекомендация**: для теста используйте `N_START = big"27"`, `MAX_ITERATIONS = 200`.

---

### 3.2. Функции ядра

| Функция                        | Назначение |
|-------------------------------|-----------|
| `split_number_str(N, m)`      | Равномерно разбивает число на `m` строк-частей. |
| `multiply_preserve_length(p, k)` | Умножает часть `p` на `k`, сохраняя исходную длину (с ведущими нулями). |
| `remove_leading_zeros(s)`     | Удаляет ведущие нули, оставляя `"0"` для нулевой строки. |
| `compare_pq_nk(pq, nk)`       | Сравнивает `PQ` и `NK` по началу/концу; возвращает тип совпадения. |
| `collatz_step(N)`             | Выполняет один шаг последовательности Коллатца. |
| `check_scs_for_collatz_step(N, m, k)` | Применяет СЧС к числу `N` и возвращает результат. |

---

### 3.3. Основная функция анализа

**`run_collatz_with_scs_analysis(N, m, k, max_steps)`**  
Выполняет полный цикл:
1. Генерирует последовательность Коллатца (до `1` или `max_steps`).
2. На каждом шаге применяет СЧС и классифицирует совпадение.
3. Сохраняет данные в `DataFrame`.
4. Формирует цветовую карту совпадений.
5. Создаёт:
   - Статический график (`PNG`),
   - Видео (`MP4`) с пошаговой динамикой,
   - Статистику (`TXT`),
   - Сырые данные (`CSV`).

---

## 📁 **4. Выходные файлы**

| Файл                                | Формат | Описание |
|------------------------------------|--------|---------|
| `collatz_scs_dynamics_full.mp4`    | MP4    | Видео с графиком: по оси X — шаг, по Y — длина числа `N`. Цвета точек отражают тип совпадения СЧС. |
| `collatz_scs_final_plot.png`       | PNG    | Финальный статический график в научном стиле: сетка, оси, легенда, рамка. |
| `collatz_scs_results.csv`          | CSV    | Полные данные по каждому шагу: `step`, `N`, `parts`, `multiplied_parts`, `PQ`, `NK`, `result`. |
| `collatz_scs_statistics.txt`       | TXT    | Итоговая статистика: количество каждого типа совпадения, параметры запуска, ссылки на файлы. |

---

## 🎨 **5. Визуализация**

### 5.1. Цветовая кодировка

| Цвет        | Тип совпадения               | Значение |
|-------------|-----------------------------|---------|
| 🔴 Красный   | `✅ Полное совпадение`       | `PQ == NK` |
| 🟠 Оранжевый| `🔄 Совпадение только конца`  | Совпадает только суффикс |
| 🔵 Синий    | `🔄 Совпадение начала и конца`|Совпадают префикс и суффикс |
| ⚪ Серый    | `❌ Нет совпадений`           | Нет общих префикса/суффикса |

### 5.2. Элементы графика

- **Сетка**: включена (`grid = true`) — для точного считывания значений.
- **Рамка**: `framestyle = :box` — чёткое выделение области графика.
- **Легенда**: `legend = :topright` — расшифровка цветов.
- **Оси**: подписаны с единицами измерения (`"Длина числа N (цифр)"`).
- **Траектория**: полупрозрачная линия, соединяющая точки.

---

## 🛠️ **6. Требования к системе**

### 6.1. Зависимости Julia
Установите командой:
```julia
using Pkg
Pkg.add(["CSV", "DataFrames", "Plots", "FFMPEG"])
```

### 6.2. Системные зависимости
- **FFmpeg**: необходим для сборки видео.  
  Установка:
  - **Windows**: скачать с [ffmpeg.org](https://ffmpeg.org), добавить в `PATH`.
  - **Linux**: `sudo apt install ffmpeg`
  - **macOS**: `brew install ffmpeg`

### 6.3. Среда выполнения
VS Code, Version 1.1 и выше.
- **Терминал**: программа **не выводит ничего** в `stdout`/`stderr` (даже предупреждения GKS подавлены).
- **GUI**: не требуется — все графики сохраняются в файлы.

---

## ▶️ **7. Пример использования**

### 7.1. Быстрый тест
Замените в коде:
```julia
const N_START = big"27"
const MAX_ITERATIONS = 200
```
Запустите:
```bash
julia collatz_scs.jl
```
Результат: видео ~40 секунд, 111 шагов, все и везде совпадения СЧС.

### 7.2. Полноценный запуск
Оставьте:
```julia
const N_START = big"9"^big"999"
const MAX_ITERATIONS = 1000
```
⚠️ **Внимание**: начальное число имеет ~954 цифр. Первые шаги будут медленными.

---

## 🔒 **8. Особенности реализации**

- **Безопасность**: использование `Atomic{Int}` позволяет легко распараллелить анализ в будущем.
- **Масштабируемость**: обработка чисел произвольной длины через `BigInt` и строковые операции.
- **Чистота вывода**: перенаправление `stderr` в `/dev/null` или `NUL` гарантирует **абсолютную тишину в терминале**.
- **Научная строгость**: графики соответствуют стандартам публикаций (сетка, подписи, легенда).

---

## 📜 **9. Лицензия и распространение**

Код является частью исследовательской работы автора и не может быть использован, без письменного согласия:  
  **Ющенко Михаил Юрьевич (Yushchenko Mikhail Yuryevich)**.
В соответствии с лицензией **All Rights Reserved.** 

---

> ✨ **Заключение**:  
> Эта программа — инструмент для **объективной демонстрации** структурной числовой симметрии в последовательности Коллатца.  
> Она сочетает **математическую строгость**, **визуальную наглядность** и **техническую чистоту**.
